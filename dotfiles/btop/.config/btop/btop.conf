#? Dracula theme. btop fills in every other key with its defaults and rewrites
#? this file on exit, so it will grow the first time you run btop.
color_theme = "/usr/share/btop/themes/dracula.theme"
theme_background = true
